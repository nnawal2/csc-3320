Please download all .c files and compile using the command 
	cc -o filename filename.c
	for example: cc -o SortElementsOfArray SortElementsOfArray.c

	Then run each code as follows:

1.

  ./SortElementsOfArray

2.

  ./alphabeticalOrderOfElementsInArray

3.
  Step1:
  To compile the program please run the following the code:
	cc -o SortElementsOfArray2 SortElementsOfArray2.c -std=c99
  Step2: to run the program:
  ./SortElementsOfArray2

4.

  ./CovidRegistration

5. 
  Step 1: download midTerm2.txt into your directory
  Step 2: change directory in the code where you have the midTerm2.txt	
  Step 3: now compile count.c using the command "cc -o count count.c"
  Step 4: run program using the command "./count >> resultFile.txt"
  Step 5: to see output of the code, run resultFile.txt using "cat resultFile.txt"

6. 

  Step1: run compiled file
	./countheader >> resultFile1.txt
  Step2: to see header of the resultFile1.txt run the following command
	sed '1i Characters, Words, Lines' resultFile1.txt

